import React from 'react'

const AccountSettings = () => (
	<div className="page">
		<h2>Account Settings</h2>
		<p>Placeholder page for Account Settings.</p>
	</div>
)

export default AccountSettings
